# v0.1.15

`codex-channels` v0.1.15 makes the Codex-visible skill execution-first for obvious operator intents, so Codex is more likely to run the local workflow instead of only describing it.

## Highlights

- make the `codex-channels` skill run-first for doctor, demo, pending, reply-latest, and targeted reply intents
- keep the generated canonical skill aligned with the repo skill definition
- continue the shift from descriptive guidance toward an operator workflow inside Codex
