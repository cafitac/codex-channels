# v0.1.12

`codex-channels` v0.1.12 fixes the Codex-visible install path by writing a canonical `codex-channels` skill into the Codex skill root during bootstrap.

## Highlights

- install `codex-channels` into the canonical Codex skill root during `plugin-bootstrap`
- keep the plugin wrapper and MCP artifacts as secondary packaging outputs
- update bootstrap tests and install docs around the current Codex-visible path
