# v0.1.20

`codex-channels` v0.1.20 fixes invalid SKILL.md frontmatter in the installed shortcut skills by quoting the `[CODEX-CHANNELS]` descriptions so Codex can load them correctly.

## Highlights

- quote `[CODEX-CHANNELS]` description strings in repo and generated skill content
- keep the visual grouping prefix while restoring valid YAML parsing
- ensure plugin-bootstrap installs shortcut skills that Codex can actually load
