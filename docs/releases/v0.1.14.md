# v0.1.14

`codex-channels` v0.1.14 makes the Codex-visible skill more operator-oriented so Codex sessions guide the local runtime loop more actively.

## Highlights

- teach the `codex-channels` skill to bias toward doctor, pending, demo, and reply-latest as the next operator steps
- keep the generated canonical skill content aligned with the repo skill definition
- preserve the current CLI-assisted model while making the Codex workflow less descriptive and more actionable
