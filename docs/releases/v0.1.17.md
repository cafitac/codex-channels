# v0.1.17

`codex-channels` v0.1.17 makes `operator-status` friendlier for Codex-guided use by printing a compact human summary by default and keeping `--json` as the structured fallback.

## Highlights

- make `operator-status` human-readable by default for the common Codex workflow
- keep `operator-status --json` for machine-readable automation and tooling
- strengthen the docs around `operator-status` as the first operator check
